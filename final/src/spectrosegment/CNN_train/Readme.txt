This directory is for CNN training files(training with only verified data) 

After preprocessing data, there are "train_X_verified.npy" and "train_Y_verified.npy",
then you can execute the following command to produce 14 models.
>bash cnn_train.sh
